这次补充不是方法变更，而是 week7 之后服务器执行 backbone 的同步切换。原来 week7 落地时，服务器本地主实验后端默认写成 Llama 3.1 8B Instruct，主要目的是先把 local HF backend、batch/registry、baseline matrix 和 medium-scale handoff 搭起来。现在这些框架已经完成，真正需要调整的是服务器侧主模型获取与加载路径：由于当前服务器访问 Hugging Face 不稳定，而 ModelScope 访问已经确认可用，后续默认服务器主实验模型切换为 Qwen3-8B，并统一通过 ModelScope 下载到服务器本地目录，再由 `local_hf_backend.py` 以 `local_files_only` 方式加载。这个切换不会改变 Part5/Part6 的方法主线，不会重开 ranking family 搜索，也不会改变 week6 收敛出的 structured risk current best family 的身份。

配置层面，今天新增了 `configs/model/qwen3_8b_local.yaml`，并把 `configs/model/local_llm.yaml` 的默认本地模型也切到 Qwen3-8B。新配置显式记录 `download_source: modelscope`、`modelscope_model_id: Qwen/Qwen3-8B`，默认本地路径为 `/home/ajifang/autodl-tmp/models/Qwen3-8B`，同时继续保留 `local_files_only: true`、`adapter_path` 预留、base-only first 和 chat template 路径。这里没有把任何 ModelScope 账号、token、服务器密码或临时凭据写进仓库；仓库只记录可复现的本地模型目录约定。旧的 Llama 配置没有被当作结果文件硬改，它可以作为历史执行骨架保留，但从现在开始默认 smoke、medium-scale 和 workflow 都以 Qwen3-8B local backend 为准。

执行入口层面，今天新增了 `beauty_qwen3_local_pointwise_smoke.yaml`、`beauty_qwen3_local_rank_smoke.yaml`、`beauty_qwen3_local_pairwise_smoke.yaml` 三条 smoke config，并新增了 `beauty_qwen3_local_pointwise_medium.yaml`、`beauty_qwen3_local_rank_medium.yaml`、`beauty_qwen3_local_pairwise_medium.yaml`、`beauty_qwen3_local_structured_risk_medium.yaml` 四条 medium-scale config。`configs/batch/week7_local_scale.yaml` 和 `configs/batch/week7_medium_scale.yaml` 已经全部切到 Qwen3-8B 默认入口；其中 medium-scale batch 仍然保留 structured risk current best rerank，并显式记录 `method_family=structured_risk_family`、`method_variant=nonlinear_structured_risk_rerank`、`is_current_best_family=true`。这说明执行 backbone 变了，但方法身份没有变：structured risk 仍然是主线，pairwise 仍然是 mechanism line，local swap 与 fully fused 仍然只是 retained exploratory family。

文档层面，README、week7 backend notes、server workflow、server execution、day5 server run plan、day3/day4 baseline 说明和 week7 工作计划都已经同步为 Qwen3-8B 当前默认口径。文档里明确写了这只是执行层变更，不代表 Part5 方法结论或 Part6 baseline 叙事被推翻。新的服务器推荐命令形态是先用 ModelScope 将模型下载到 `/home/ajifang/autodl-tmp/models/Qwen3-8B`，再执行 `main_backend_check.py --model_config configs/model/qwen3_8b_local.yaml`，通过后再用 `main_batch_run.py --batch_config configs/batch/week7_local_scale.yaml --run` 或 `configs/batch/week7_medium_scale.yaml --run` 进入真实 batch。LoRA 仍然不在本轮强行启动，后续只在需要适配时以 adapter 形式接入；vLLM 也继续作为吞吐进一步扩大后的可选服务化路线。

本地验证上，我已经执行了 Qwen3 backend dry-run、local-scale batch dry-run 和 medium-scale batch dry-run。`outputs/summary/week7_qwen3_backend_check_dry_run.csv` 显示 pointwise、candidate ranking、pairwise 三条任务线都已经通过 Qwen3 local config 进入 backend check schema；`outputs/summary/week7_day2_batch_status.csv` 显示三条 smoke 任务均为 `dry_run_ready`；`outputs/summary/week7_day5_batch_status.csv` 显示 medium pointwise、direct rank、pairwise 均为 `dry_run_ready`，structured risk current best rerank 仍为 `input_missing`，这是正确状态，因为它依赖服务器先生成 Qwen3 medium rank prediction 和 calibrated uncertainty。这个边界需要继续保持诚实：当前完成的是执行入口切换和 dry-run 可运行性确认，真实 GPU 加载与非 dry-run 指标仍需在服务器上执行后再写入结果叙事。
