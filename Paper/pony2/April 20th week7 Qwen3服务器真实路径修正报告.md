这次同步的是服务器真实环境配置修正，不是方法变更。服务器上重新运行 `main_backend_check.py --model_config configs/model/qwen3_8b_local.yaml` 时暴露出的失败原因，不是 Qwen3-8B 模型不可用，也不是 local HF backend、chat template 或 parser/thinking cleanup 的逻辑错误，而是仓库里原先写入的模型本地路径与当前服务器真实 ModelScope 下载路径不一致。当前服务器已经验证可本地加载的 Qwen3-8B 目录是 `/home/ajifang/models/Qwen/Qwen3-8B`，因此本轮只把执行层配置吸收到仓库中，不触碰 Part5/Part6 的方法主线，也不重新打开 ranking family 搜索。

具体修正集中在 Qwen3 本地模型配置与 week7 服务器运行文档。`configs/model/qwen3_8b_local.yaml` 和 `configs/model/local_llm.yaml` 中的 `model_name_or_path`、`tokenizer_name_or_path` 已统一改为 `/home/ajifang/models/Qwen/Qwen3-8B`。这意味着后续默认 Qwen3 local backend 会直接命中当前服务器真实已验证目录，并继续保持 `local_files_only: true`、`enable_thinking: false`、base-only first 与 adapter_path 预留路线。`configs/exp` 和 `configs/batch` 本身没有硬编码模型目录，它们只引用 `configs/model/qwen3_8b_local.yaml` 或 `qwen3_8b_local` 这个模型身份，因此不需要逐个改实验配置。`docs/week7_day1_backend_notes.md`、`docs/week7_local_backend_and_server_workflow.md`、`docs/week7_server_execution.md` 和 `docs/week7_day5_server_run_plan.md` 也同步写清楚：当前机器实际使用的是 `/home/ajifang/models/Qwen/Qwen3-8B`，这是服务器路径修正，不是研究结论变化。

这次我也重新检查了旧路径残留。旧的 Qwen3 placeholder 路径不应再作为默认下载命令或默认运行路径出现在 `configs/model`、`configs/exp`、`configs/batch`、`docs`、`README` 或 smoke/server workflow 脚本里。保留的只有研究身份与执行身份本身：Qwen3-8B 仍是 week7 之后服务器本地主实验 backbone，structured risk 仍然是 week6 收敛出的 current best ranking family，pairwise 仍然是 mechanism line，local swap 和 fully fused 仍然作为 retained exploratory family 保留。换句话说，路径修正只解决“服务器从哪里加载 Qwen3”，不改变“我们在实验里验证什么方法”。

同时，服务器四域 processed 数据已经就位：`data/processed/amazon_beauty`、`data/processed/amazon_books_small`、`data/processed/amazon_electronics_small`、`data/processed/amazon_movies_small`。这说明下一步不需要回头改数据、不需要重跑 preprocess，也不需要基于旧小数据硬跑；正确顺序仍然是先在服务器项目根目录重新执行 Qwen3 backend check，再执行 `RUN_SMOKE=1 bash scripts/week7_day1_server_backend_check.sh "$PWD"`，确认 pointwise、candidate ranking、pairwise 三条 smoke 的 parse success 正常，然后再进入 `python main_batch_run.py --batch_config configs/batch/week7_local_scale.yaml --run`。这保持了 week7 的执行层推进逻辑：先真实路径，后真实 smoke，再真实 batch。
