今天我把 week7 第五天定位为服务器本地主实验后端的 medium-scale 承接日，而不是继续新增方法公式。这个判断和 week7 的整体目标是一致的：week5 和 week6 已经把 Part5 的多任务不确定性感知方法闭环做成，week7 前四天已经把本地 HF 后端、batch/registry、literature-aligned baseline 和三层 baseline matrix 建起来；如果第五天继续只写方法或只扩文档，Part6 中“摆脱 API 速率和成本瓶颈”的执行层主张仍然会悬空。因此今天的核心不是在本地伪造服务器结果，而是把 Beauty medium-scale 的服务器运行配置、batch 清单、current best family 承接、summary 入口和 base-only/LoRA/vLLM 路线全部落进仓库，让后续上服务器时可以直接按统一入口真实运行。

工程上，今天补齐了四个中等规模配置：`configs/exp/beauty_llama31_local_pointwise_medium.yaml`、`configs/exp/beauty_llama31_local_rank_medium.yaml`、`configs/exp/beauty_llama31_local_pairwise_medium.yaml` 和 `configs/exp/beauty_llama31_local_structured_risk_medium.yaml`。这四个配置统一使用 `configs/model/llama31_8b_instruct_local.yaml`，因为 week7 当前主实验后端已经固定为 Llama 3.1 8B Instruct，而不是继续沿用早期计划里的 qwen_local 命名。它们被统一纳入 `configs/batch/week7_medium_scale.yaml`，形成 pointwise diagnosis、direct candidate ranking、structured risk current best rerank、pairwise preference 四条执行线。这里最关键的是 structured risk current best rerank 被正式写入 batch，并显式标记 `method_family=structured_risk_family`、`method_variant=nonlinear_structured_risk_rerank`、`is_current_best_family=true`，这说明 week6 收敛出的主线已经进入 week7 的执行系统，而不是只停留在 summary 结论里。

为了落实补充说明里 family-aware 的要求，我还修改了 `src/utils/exp_launcher.py` 和 `src/utils/exp_registry.py`。现在 batch/registry 除了记录 `task`、`model`、`domain`、`status`、`output_dir` 之外，还会记录 `method_family`、`method_variant`、`is_current_best_family` 和 `prediction_ready`。同时，`main_rank_rerank.py` 新增了 config 入口，`exp_launcher` 也能识别 `candidate_ranking_rerank` 任务并调用 `main_rank_rerank.py`。这一步很重要，因为如果 structured risk 只能靠手工命令跑，而不能进入 batch 和 registry，那么它就不算真正成为 week7 的主实验承接线。今天还新增了 `src/analysis/aggregate_week7_medium_scale.py` 与 `main_compare_week7_medium_scale.py`，用来把 medium batch 状态、后续真实指标和 day4 baseline matrix 聚合成 `outputs/summary/week7_day5_medium_scale_summary.csv`。

今天我在本地执行了 medium-scale batch 的 dry-run/status 检查，而没有启动 GPU 推理。`outputs/summary/week7_day5_batch_status.csv` 显示 pointwise、direct candidate ranking 和 pairwise 三项为 `dry_run_ready`，说明输入文件存在、命令可以构建、服务器上加 `--run` 后可以进入真实执行；structured risk current best rerank 当前是 `input_missing`，这是预期且正确的状态，因为它依赖服务器先生成 `outputs/beauty_llama31_local_rank_medium/predictions/rank_predictions.jsonl`，同时还需要 pointwise 校准后得到 `outputs/beauty_llama31_local_pointwise_medium/calibrated/test_calibrated.jsonl`。我没有把这个依赖状态写成“已完成 rerank”，而是让 registry 诚实记录等待上游产物。随后我运行 `main_compare_week7_medium_scale.py`，生成 `outputs/summary/week7_day5_medium_scale_summary.csv`，当前共 22 行，其中前几行是服务器 medium batch 的 handoff 状态，后续行是来自 day4 baseline matrix 的 reference 对照。这张表当前的意义是服务器真实运行前的 summary shell，等服务器非 dry-run 和评测完成后，会自动吸收真实指标。

今天还新增了 `docs/week7_day5_server_run_plan.md`，把后续路线写死为 base-only first。具体来说，第一步先确认服务器 conda 环境、CUDA、Transformers、Accelerate 和 Llama 3.1 8B Instruct 本地缓存；第二步先跑 base-only inference baseline；第三步只有在 week8 或 reviewer-gap 结果显示任务确实需要适配时，才引入 PEFT 做 LoRA 或 QLoRA，而且只保存 adapter，不复制或重训整份 base model；第四步推理阶段优先使用 Transformers 直接加载 base 或 `base + adapter`，只有当吞吐成为主要瓶颈时，再考虑 vLLM 常驻服务。这个顺序能避免过早引入训练复杂度，也能防止后面团队在“先训不训练、模型放哪里、推理怎么跑”这些问题上反复摇摆。README 也同步更新了 medium-scale handoff、batch 清单、summary 入口和服务器运行计划，但保持项目级说明，没有写成可复制的细粒度教程。

今天对补充说明的落实可以明确确认。第一，week7 服务的主线已经不是模糊的 uncertainty-aware rerank，而是 week6 收敛出的 structured risk current best family；这一点已经进入 `configs/batch/week7_medium_scale.yaml` 和 registry schema。第二，pairwise-to-rank 继续作为 mechanism line，今天只把 pairwise medium-scale 放进 batch，不把它写成主决策替代。第三，local swap 和 fully fused 没有被删除，但也没有被拉回默认 medium-scale 主实验资源；它们继续留在 Part5 表和 baseline matrix 中作为 retained exploratory family。第四，API 组、本地 HF 主模型组、未来 LoRA-adapted 组的身份边界继续保持清楚：API 负责观察，本地 HF 负责主实验吞吐，LoRA 只作为后续 adapter 扩展。第五，今天没有伪造服务器运行结果，本地只完成 dry-run 和 handoff，真实非 dry-run 将在服务器环境确认后执行。这个状态是顶会级项目管理需要的诚实边界：代码入口、配置、状态表和路线已经完整，结果指标必须等真实服务器运行后再写。

week7 到今天为止的整体价值已经比较清楚。它不只是新增了 backend、batch 或 baseline 文件，而是把 version pony 从 Part5 的方法闭环推进到了 Part6 的执行层骨架：服务器本地主后端已经有模型配置和 workflow，batch/registry 已经能组织多任务并追踪失败，literature-aligned baseline 已经有第一轮可运行入口，三层 baseline matrix 已经统一，medium-scale 服务器 handoff 已经具备点、排、对和 structured risk current best 四条线，base-only → LoRA-adapted → vLLM serving 的后续路线也已经固定。下一步真正应该做的是上服务器跑非 dry-run，而不是在本地继续开新公式。只要服务器结果补齐，week7 就能自然承接到 week8 的多域、多模型、多噪声统一验证矩阵。
