project: collm
official_repo: https://github.com/zyang1580/CoLLM
seed: 13
output_dir: outputs/server_packets/collm_movielens_1m
upstream_status: clone_required
execution_status: packet_ready_only
dataset:
  name: movielens_1m_r2
  processed_dir: data/processed/movielens_1m/r2_full_single_dataset
candidate:
  protocol: sampled
  size: 500
  include_target: true
collm:
  task: candidate_ranking
  max_history_items: 50
  expected_external_output: candidate_scores.csv
  notes: CoLLM collaborative-embedding adapter packet; record embedding alignment and checkpoint details before official comparison.
