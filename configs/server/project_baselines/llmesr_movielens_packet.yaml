project: llmesr
official_repo: https://github.com/liuqidong07/LLM-ESR
seed: 13
output_dir: outputs/server_packets/llmesr_movielens_1m
upstream_status: clone_required
execution_status: packet_ready_only
dataset:
  name: movielens_1m_r2
  processed_dir: data/processed/movielens_1m/r2_full_single_dataset
candidate:
  protocol: sampled
  size: 500
  include_target: true
llmesr:
  task: long_tail_sequential_candidate_ranking
  max_history_items: 50
  expected_external_output: candidate_scores.csv
  notes: Long-tail/sequential packet. Keep TRUCE long-tail slices and final evaluator as the only paper metric source.
