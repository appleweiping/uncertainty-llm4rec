project: llara
controlled_baseline_name: llara_qwen3_adapter_amazon_beauty
seed: 13
base_model: /home/ajifang/models/Qwen/Qwen3-8B
base_model_policy: shared_qwen3_8b_base_model
adapter_training_policy: baseline_official_algorithm_specific_adapter
implementation_fidelity: controlled_adapter_pilot
official_repo: https://github.com/ljy0ustc/LLaRA
packet_dir: outputs/server_packets/llara_amazon_beauty
output_dir: outputs/server_training/controlled_baselines/llara_qwen3_adapter_amazon_beauty
max_train_examples: null
max_valid_examples: null
negatives_per_train_example: 15
training:
  num_train_epochs: 1
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 1
  gradient_accumulation_steps: 16
  learning_rate: 0.0002
  warmup_ratio: 0.03
  weight_decay: 0.0
  bf16: true
  gradient_checkpointing: true
lora:
  r: 16
  alpha: 32
  dropout: 0.05
  target_modules: [q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj]
scoring:
  type: official_native_candidate_scoring_required
  output: candidate_scores.csv
paper_policy:
  shared_base_model: Qwen3-8B
  adapter_policy: baseline_official_algorithm_specific_adapter
  current_claim_label: official_native_required_before_main_table
  official_project_checkpoint: false
  main_table_after_real_training_only: true
provenance:
  official_algorithm_reused: false
  official_native_upgrade_required: true
  recommended_role: main_official_baseline
  notes: LLaRA should reuse official recommendation-signal alignment modules before main-table use.
