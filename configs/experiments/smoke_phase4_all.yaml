seed: 13
run_name: smoke_phase4_all
output_dir: outputs/runs
domain: tiny
dataset:
  name: tiny
  config_path: configs/datasets/tiny.yaml
  processed_dir: data/processed/tiny/phase1
split: test
candidate:
  protocol: full
  k: null
  seed: 13
baselines: [sequential_last_item, sequential_markov, sasrec_interface]
training:
  config_path: configs/training/sequential_tiny.yaml
evaluation:
  top_k: [1, 5, 10]
top_k: [1, 5, 10]
logging:
  level: INFO
output:
  run_name: smoke_phase4_all
  output_dir: outputs/runs
