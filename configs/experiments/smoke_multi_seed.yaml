seed: 13
seeds: [13, 17]
run_name: smoke_multi_seed
output_dir: outputs/runs
domain: tiny
dataset:
  name: tiny
  config_path: configs/datasets/tiny.yaml
  processed_dir: data/processed/tiny/phase1
split: test
candidate:
  protocol: full
  k: null
  seed: 13
baselines: [popularity, sequential_markov]
training:
  config_path: configs/training/sequential_tiny.yaml
evaluation:
  config_path: configs/evaluation/full.yaml
  top_k: [1, 5, 10]
top_k: [1, 5, 10]
aggregate:
  enabled: true
  output_dir: outputs/tables
logging:
  level: INFO
output:
  run_name: smoke_multi_seed
  output_dir: outputs/runs
