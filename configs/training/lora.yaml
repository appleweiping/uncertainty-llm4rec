base_model_name_or_path: local-placeholder-model
output_dir: outputs/runs/smoke_lora_dry_run_seed13/artifacts/lora_dry_run
dataset: tiny
peft_method: lora
rank: 4
alpha: 8
dropout: 0.05
target_modules: [q_proj, v_proj]
max_steps: 1
epochs: null
dry_run: true
notes: "Safe Phase 4 dry-run config only; does not download models or run LoRA training."
