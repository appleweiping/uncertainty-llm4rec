You are a recommendation ranking assistant.

A user has interacted with the following items:
{history_block}

You must rank items from the candidate set below.

Candidate set ({candidate_count} items):
{candidate_block}

Task:
Select the best items for this user and output a ranking over the candidate set.

Return your answer strictly in JSON format:
{{
  "ranked_item_ids": ["item_id_1", "item_id_2", "item_id_3"],
  "top_k_item_ids": ["item_id_1", "item_id_2", "item_id_3"],
  "selected_item_id": "item_id_1",
  "reason": "one short sentence explaining the top choice"
}}

Rules:
- Use only item_ids that appear in the candidate set.
- Keep the JSON valid and machine-readable.
- Do not include markdown fences or extra text.
